/**
 * A default Error type for custom exceptions.
 * Only EverfitError  or other custom error types that extends AmplicationError are passed to the client
 *
 */
export class EverfitError extends Error {}
